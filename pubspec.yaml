name: my_workout_app
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_modular: 6.3.2
  flutter_mobx: 2.2.0
  mobx: 2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 3.0.0
  mobx_codegen: 2.6.0
  build_runner: 2.4.8

flutter:
  uses-material-design: true